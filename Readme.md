it's a simple webapp to get capture feedback via an acquisition card like Mirabox. 

It uses the ```getUserMedia``` browser system provided by electron. 

The project is in WIP it can be used anytime. 
